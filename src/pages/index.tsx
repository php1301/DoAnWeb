import React from "react";

import { Container, Main, Cards } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <Main />
            <Cards />
        </Container>
    );
};

export default Home;
